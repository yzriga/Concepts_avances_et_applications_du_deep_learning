# TP4/configs/sage_sampling.yaml
seed: 42
device: "cuda"
epochs: 200
lr: 0.01
weight_decay: 5.0e-4

sage:
  hidden_dim: 256
  dropout: 0.5

sampling:
  batch_size: 64
  num_neighbors_l1: 25
  num_neighbors_l2: 10
